[pytest]
# Analyzer suite plus the vendored Trading Desk suite (desk/tests). desk/src makes `optionsdesk`
# importable without a pip install, matching backend.trading_desk's runtime bootstrap.
testpaths = tests desk/tests
pythonpath = . desk/src
filterwarnings =
    ignore::DeprecationWarning
